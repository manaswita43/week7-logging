apiVersion: v1
kind: Service
metadata:
  name: iris-api-service
  labels:
    app: iris-api
spec:
  selector:
    app: iris-api
  type: LoadBalancer
  ports:
    - name: http
      port: 8200
      targetPort: 8200
